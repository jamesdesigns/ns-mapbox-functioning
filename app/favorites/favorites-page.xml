<!-- Original Code Below -->
<!-- <Page
    class="page"
    navigatingTo="navigatingTo"  
    xmlns="http://schemas.nativescript.org/tns.xsd"
    xmlns="http://www.nativescript.org/tns.xsd"
    xmlns:lv="nativescript-ui-listview"
    >
    -->

<Page
    class="page"
    navigatingTo="onNavigatingTo"  
    xmlns="http://schemas.nativescript.org/tns.xsd"
    xmlns="http://www.nativescript.org/tns.xsd"
    xmlns:lv="nativescript-ui-listview"
    >

    <ActionBar class="action-bar">
        <Label class="action-bar-title" text="Favorites"></Label>
    </ActionBar>
    <!-- <ActionBar class="action-bar">
        <GridLayout class="action-bar-title" widht="100%">
        <Label text="Favorites"></Label>
        <StackLayout orientation="horizontal" horizontalAlignment="right" margin="0 15 0 15">
            <Image src="~/images/fav-outline.png" height="30" tap="{{ toggleFavouritesFilter }}" margin="0 5 0 5"></Image>
            <Image src="~/images/layout-grid.png" height="30" tap="{{ changeLayout }}" margin="0 5 0 5"></Image>
         </StackLayout>
         </GridLayout>   
    </ActionBar> -->

        <StackLayout class="p-20">
        <Label text="My Favorites" class="h2 text-center"/>
          <Button text="&#xff08a;   Provo" tap="{{ onTap }}" class="my-button text-left" />
          <Button text="&#xff08a;   Orem Central" tap="{{ onTap }}" class="my-button text-left" />
          <Button text="&#xff08a;   American Fork" tap="{{ onTap }}" class="my-button text-left" />
          <Button text="&#xff08a;   Lehi" tap="{{ onTap }}" class="my-button text-left" />
          <Button text="&#xff004;   Draper" tap="{{ onTap }}" class="my-button text-left" />
          <Button text="&#xff08a;   South Jordan" tap="{{ onTap }}" class="my-button text-left" />

        <Button text="Add To Favorites" tap="{{ onTap }}" class="btn btn-primary btn-active"/>
     
        <Label text="{{ message }}" class="h2 text-center" textWrap="true"/>
        <Label text=""/>
        <!-- <Label text="Using LocalStorage to store the count, quit and re-open to see it saved!" textWrap="true"/> -->
    
    </StackLayout>

  

</Page>